# 游戏开发技能全面测试计划

> **用户选择**: 优先塔防游戏 + 需要多人功能测试
> **使用技能**: game-development-orchestrator + phaser-gamedev + game-automation-testing

---

## 当前状态

### 已完成的游戏
| 游戏 | 类型 | 复杂度 | 技术要点 |
|------|------|--------|----------|
| Neon Fall 100 | 平台跳跃/下落 | 中等 | 7种平台、4种能力、Boss战、成就系统 |
| Pong | 体育竞技 | 简单 | 基础物理、AI对手 |
| 像素风横板冒险 | 横版动作 | 中等 | 战斗系统、关卡管理、Boss系统（开发中） |

### 本次测试的技能覆盖

| 技能 | 用途 | 验证点 |
|------|------|--------|
| **game-development-orchestrator** | 完整工作流调度 | 工作流执行、技能协调 |
| **phaser-gamedev** | Phaser 3 开发 | 场景架构、物理、TDD |
| **game-asset-generator** | 素材生成 | 塔/敌人精灵、UI图标 |
| **game-audio-generator** | 音频生成 | 攻击音效、背景音乐 |
| **game-automation-testing** | 自动化测试 | AI自动玩、验证逻辑 |
| **game-browser-tester** | AI测试 | AI自动玩游戏验证 |

### 新测试的机制
- ❌ **A*路径规划** ⬅️ 塔防核心
- ❌ **经济/资源系统** ⬅️ 塔防核心
- ❌ **波次管理系统** ⬅️ 塔防核心
- ❌ **多人/网络功能** ⬅️ Socket.io

---

## 开发计划（基于 game-development-orchestrator full-production 工作流）

### Phase 1: 创意头脑风暴 (superpowers:brainstorming)
**目标**: 确定游戏设计方向

游戏概念：
- **名称**: Pixel Tower Defense（像素塔防）
- **类型**: 策略/塔防 + 多人合作
- **风格**: 像素风
- **目标平台**: Web (Phaser 3)

---

### Phase 2: 详细规划 (superpowers:writing-plans)
**输出**: GDD（游戏设计文档）

#### 核心玩法
```
1. 敌人从起点沿着路径走向终点
2. 玩家在网格上放置防御塔
3. 击杀敌人获得金币
4. 每波敌人越来越强
5. 敌人到达终点扣生命
6. 生命归零则游戏结束
```

#### 塔类型设计
| 塔 | 攻击方式 | 特性 | 费用 |
|----|---------|------|------|
| 箭塔 | 单体远程 | 快速、低伤害 | 50 |
| 魔法塔 | 范围伤害 | 溅射、中等伤害 | 100 |
| 炮塔 | 高伤害 | 慢速、AOE | 150 |
| 减速塔 | 无伤害 | 减速敌人50% | 75 |

#### 敌人类型设计
| 敌人 | 生命 | 速度 | 特性 | 奖励 |
|------|------|------|------|------|
| 小怪 | 50 | 正常 | 无 | 10 |
| 快速 | 30 | 快 | 速度快 | 15 |
| 坦克 | 200 | 慢 | 高生命 | 30 |
| Boss | 1000 | 慢 | 每N波出现 | 100 |

---

### Phase 3: 搜索最佳实践 (web_search)
**查询**:
- "tower defense game mechanics best practices"
- "phaser 3 tower defense tutorial"
- "A* pathfinding javascript implementation"
- "socket.io multiplayer game sync"

---

### Phase 4: 项目脚手架 (phaser-gamedev scaffold)
**使用模板**: phaser-vite（含 PlaceholderFactory）

```
pixel-tower-defense/
├── src/
│   ├── main.js
│   ├── config.js
│   ├── scenes/
│   │   ├── BootScene.js
│   │   ├── MenuScene.js
│   │   ├── GameScene.js
│   │   ├── UIScene.js          # 并行场景
│   │   └── GameOverScene.js
│   ├── entities/
│   │   ├── Tower.js            # 塔基类
│   │   ├── ArrowTower.js
│   │   ├── MageTower.js
│   │   ├── CannonTower.js
│   │   ├── SlowTower.js
│   │   └── Enemy.js            # 敌人类
│   ├── systems/
│   │   ├── Pathfinding.js      # A*寻路
│   │   ├── WaveManager.js      # 波次管理
│   │   ├── Economy.js          # 经济系统
│   │   ├── Projectile.js       # 弹道系统
│   │   └── DebugSystem.js      # 调试系统
│   ├── ui/
│   │   ├── TowerMenu.js
│   │   ├── UpgradePanel.js
│   │   └── HUD.js
│   └── config/
│       ├── towers.js
│       ├── enemies.js
│       └── waves.js
├── server/                      # 多人服务器
│   ├── index.js
│   ├── rooms.js
│   └── game.js
├── tests/
│   ├── setup.js
│   ├── unit/
│   │   ├── Pathfinding.test.js
│   │   ├── WaveManager.test.js
│   │   └── Economy.test.js
│   └── e2e/
│       └── gameplay.test.js
├── assets/
│   ├── images/
│   ├── audio/
│   └── maps/
├── package.json
└── vite.config.js
```

---

### Phase 5: TDD 代码开发（占位符模式）
**原则**: 先写测试，再实现功能

#### 测试用例（RED 先行）

```javascript
// tests/unit/Pathfinding.test.js
describe('A* Pathfinding', () => {
  it('should find path from start to end', () => {
    const grid = [
      [0, 0, 0],
      [0, 1, 0],  // 1 = obstacle
      [0, 0, 0]
    ];
    const path = Pathfinder.findPath(grid, [0,0], [2,2]);
    expect(path.length).toBeGreaterThan(0);
    expect(path[path.length-1]).toEqual([2,2]);
  });

  it('should return empty array when no path exists', () => {
    const grid = [
      [0, 1, 0],
      [1, 1, 1],  // 完全堵死
      [0, 1, 0]
    ];
    const path = Pathfinder.findPath(grid, [0,0], [2,2]);
    expect(path).toEqual([]);
  });
});

// tests/unit/WaveManager.test.js
describe('Wave Manager', () => {
  it('should spawn enemies in waves', () => {
    const waveManager = new WaveManager();
    waveManager.startWave(1);
    expect(waveManager.enemiesToSpawn).toBeGreaterThan(0);
  });

  it('should increase difficulty each wave', () => {
    const waveManager = new WaveManager();
    const wave1Enemies = waveManager.getWaveConfig(1).count;
    const wave5Enemies = waveManager.getWaveConfig(5).count;
    expect(wave5Enemies).toBeGreaterThan(wave1Enemies);
  });
});

// tests/unit/Economy.test.js
describe('Economy System', () => {
  it('should add gold when enemy killed', () => {
    const economy = new Economy();
    economy.addGold(50);
    expect(economy.gold).toBe(100);  // 初始50 + 50
  });

  it('should deduct gold when tower built', () => {
    const economy = new Economy();
    const result = economy.spendGold(30);
    expect(result).toBe(true);
    expect(economy.gold).toBe(20);
  });

  it('should not allow spending more than available', () => {
    const economy = new Economy();
    const result = economy.spendGold(100);
    expect(result).toBe(false);
    expect(economy.gold).toBe(50);  // 不变
  });
});
```

#### 关键系统实现

**A* 寻路**:
```javascript
// src/systems/Pathfinding.js
export class Pathfinding {
  static findPath(grid, start, end) {
    // A* 算法实现
  }
}
```

**波次管理**:
```javascript
// src/systems/WaveManager.js
export class WaveManager {
  constructor(scene) {
    this.scene = scene;
    this.currentWave = 0;
    this.enemiesToSpawn = 0;
  }

  startWave(waveNumber) {
    this.currentWave = waveNumber;
    const config = this.getWaveConfig(waveNumber);
    this.enemiesToSpawn = config.count;
    this.spawnTimer = this.scene.time.addEvent({
      delay: config.spawnDelay,
      callback: () => this.spawnEnemy(config),
      repeat: config.count - 1
    });
  }
}
```

---

### Phase 6: AI 游戏测试 (game-browser-tester)
**验证**: 核心逻辑正确性

```javascript
// 自动测试流程
1. 启动游戏服务器 (npm run dev)
2. 打开浏览器
3. AI 自动操作:
   - 等待第一波敌人
   - 在合适位置放置塔
   - 验证敌人沿路径移动
   - 验证塔攻击敌人
   - 验证金币获取
   - 验证波次递增
4. 录制测试视频
5. 生成测试报告
```

---

### Phase 7: 素材生成 (game-asset-generator)
**使用 PlaceholderFactory 先行开发，后期替换**

```yaml
assets:
  towers:
    - arrow_tower: 像素风箭塔 (32x32)
    - mage_tower: 像素风魔法塔 (32x32)
    - cannon_tower: 像素风炮塔 (48x48)
    - slow_tower: 像素风减速塔 (32x32)
  enemies:
    - basic_enemy: 像素风小怪 (24x24)
    - fast_enemy: 像素风快速敌人 (20x20)
    - tank_enemy: 像素风坦克敌人 (40x40)
    - boss_enemy: 像素风Boss (64x64)
  ui:
    - gold_icon: 金币图标 (16x16)
    - heart_icon: 生命图标 (16x16)
    - wave_banner: 波次横幅 (200x40)
  effects:
    - projectile_arrow: 箭矢 (8x8)
    - projectile_magic: 魔法弹 (12x12)
    - explosion: 爆炸特效 (32x32)
  scenes:
    - grassland: 草地背景 (800x600)
```

---

### Phase 8: 音效添加 (game-audio-generator)

```yaml
audio:
  sfx:
    - tower_shoot: 塔攻击音效
    - enemy_death: 敌人死亡音效
    - coin_collect: 金币获取音效
    - wave_start: 波次开始音效
    - game_over: 游戏结束音效
    - tower_build: 建造音效
    - tower_upgrade: 升级音效
  bgm:
    - gameplay: 轻松背景音乐 (循环)
    - boss_wave: Boss战音乐 (循环)
  ui:
    - button_click: 按钮点击
    - menu_hover: 菜单悬停
```

---

### Phase 9: 代码审查 (superpowers:requesting-code-review)
**重点检查**:
- [ ] 所有变量初始化（防止 undefined/NaN）
- [ ] 测试覆盖率 ≥ 80%
- [ ] 物理碰撞边界条件
- [ ] 性能优化（对象池）

---

### Phase 10: 自动化测试 (game-automation-testing)
**测试类型**:
- E2E 测试：完整游戏流程
- 性能测试：100+敌人时帧率

---

### Phase 11: 多人功能开发（第二阶段）

#### 服务器架构
```
server/
├── index.js           # Socket.io 服务器
├── rooms.js           # 房间管理
├── game.js            # 游戏状态同步
└── matchmaking.js     # 匹配系统
```

#### Socket.io 事件
```javascript
// 客户端 → 服务器
socket.emit('create-room', { name: 'Room1' });
socket.emit('join-room', { roomId: 'xxx' });
socket.emit('build-tower', { x: 5, y: 3, type: 'arrow' });
socket.emit('start-game');

// 服务器 → 客户端
socket.emit('room-created', { roomId: 'xxx' });
socket.emit('player-joined', { playerId: 'yyy' });
socket.emit('game-state', { enemies: [], towers: [], gold: 100 });
socket.emit('enemy-spawned', { enemy: {...} });
socket.emit('tower-built', { tower: {...} });
```

#### 状态同步策略
- **权威服务器**: 所有游戏逻辑在服务器运行
- **客户端预测**: 客户端本地预测塔建造
- **状态插值**: 平滑敌人移动

---

## 调试系统（URL 参数）

```bash
# 跳到第10波，999金币
http://localhost:5173?debug=true&wave=10&gold=999

# 直接进入Boss波
http://localhost:5173?debug=true&boss=1

# 无限金币
http://localhost:5173?debug=true&infinite_gold=1
```

---

## 验证清单

### 功能测试
- [ ] 敌人沿路径移动
- [ ] 塔攻击范围内敌人
- [ ] 金币正确获取/消费
- [ ] 波次正确递增
- [ ] 游戏结束判定正确

### 技能测试
- [ ] game-development-orchestrator 工作流执行
- [ ] phaser-gamedev TDD 流程
- [ ] game-asset-generator 素材生成
- [ ] game-audio-generator 音效生成
- [ ] game-browser-tester AI 测试
- [ ] game-automation-testing 自动化测试

### 多人测试
- [ ] 房间创建/加入
- [ ] 状态同步正确
- [ ] 断线重连

---

## 开发命令

```bash
# 1. 创建项目
cd d:/游戏开发
mkdir pixel-tower-defense

# 2. 初始化（使用模板）
# 复制 phaser-vite 模板

# 3. 安装依赖
npm install phaser socket.io socket.io-client

# 4. 开发运行
npm run dev

# 5. 测试
npm test

# 6. 构建
npm run build
```

---

## 下一步行动

确认后按以下顺序执行：

1. **superpowers:brainstorming** - 游戏创意头脑风暴
2. **superpowers:writing-plans** - 详细规划
3. **phaser-gamedev scaffold** - 创建项目脚手架
4. **TDD 开发** - 先写测试再实现
5. **game-browser-tester** - AI 自动测试验证
6. **game-asset-generator** - 生成素材
7. **game-audio-generator** - 添加音效
8. **superpowers:requesting-code-review** - 代码审查
9. **game-automation-testing** - 自动化测试
10. **多人功能** - Socket.io 集成

---

**创建时间**: 2026-02-21
**计划状态**: 待执行
